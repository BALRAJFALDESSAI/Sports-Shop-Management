XAMPP

Database name:sports
 table :admin
        a_id int(11)
        user_name varchar(50)
        password  varchar(50)
        insert data into admin table through xampp to login 
 table :buy
        buy_id int(11)
        b_price int(11)
 table :customer
        c_id int(11)
        c_name varchar(50)
        c_number varchar(50)
 table :employee
        e_id int(11)
        e_name varchar(50)
        e_DOB date
        e_number varchar(50)
table :stock
        s_id int(11)
        s_name varchar(50)
        s_quantity int(50)
        s_perquantity int(50)